logging:
  level:
    org.tcp.gesipan : debug

spring:
  datasource:
    hikari:
      username: "gesipan"
      password: "gesipan"
      validation-timeout: 1000
    url: "jdbc:mysql://localhost:3307/gesipan"
  mvc:
    view:
      prefix: /WEB-INF/jsp/
      suffix: .jsp

mybatis:
  mapper-locations: "classpath:mapper/**/*.xml"
  type-aliases-package: org.tcp.gesipan.domain

